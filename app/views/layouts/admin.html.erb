<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Salon kosmetyczny</title>

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->

  <%= stylesheet_link_tag 'admin', 'data-turbolinks-track' => true %>
  <%= stylesheet_link_tag '//cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.7.2/fullcalendar.min.css'%>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag '//momentjs.com/downloads/moment-with-locales.min.js' %>
  <%= javascript_include_tag '//cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.7.2/fullcalendar.min.js' %>
  <%= javascript_include_tag '//cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.7.2/lang-all.js' %>
  <script src="/assets/jquery.validationEngine-pl.js" type="text/javascript" charset="utf-8"></script>
  <script src="/assets/jquery.validationEngine.js" type="text/javascript" charset="utf-8"></script>
  <link rel="stylesheet" href="/assets/validationEngine.jquery.css" type="text/css"/>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Przełącz nawigację</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Salon kosmetyczny' , root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if employee_signed_in? %>
        <li><%= link_to('Wyloguj', destroy_employee_session_path, :method => :delete) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<% if employee_signed_in? %>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
      <ul class="nav nav-sidebar">
        <li><a style="font-weight: bold;color: #fff; background: #222" class="text-center">Witaj <%= current_employee.first_name %></a></li>
        <%= nav_link 'Strona główna panelu', admin_index_path %>
        <%= nav_link 'Pracownicy', admin_employees_index_path %>
        <%= nav_link 'Wizyty', admin_visits_index_path %>
        <%= nav_link 'Usługi', admin_service_index_path %>
        <%= nav_link 'Komentarze', admin_comments_index_path %>
      </ul>
    </div>
    <div class="col-sm-9 col-md-10 main">
      <% if alert %>
      <div class="alert alert-warning"><%= alert %></div>
      <% end %>
      <% if notice %>
      <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <%= yield %>
    </div>
  </div>
</div>
<% else %>
<div class="col-md-4 col-md-offset-4" style="margin-top: 20px">
  <% if alert %>
      <div class="alert alert-warning"><%= alert %></div>
  <% end %>
  <% if notice %>
      <div class="alert alert-success"><%= notice %></div>
  <% end %>
  <%= yield %>
</div>


<% end %>
</body>
</html>
